interface:
  display_name: "Agent Workspace Checkpoint"
  short_description: "Create encrypted, zero-knowledge workspace snapshots"
  default_prompt: "Use $agent-workspace-checkpoint to create and locally encrypt a sanitized checkpoint of this project for handoff."
