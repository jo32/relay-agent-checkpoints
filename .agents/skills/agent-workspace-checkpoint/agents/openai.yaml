interface:
  display_name: "Agent & Skill Checkpoint"
  short_description: "Save or publish agents and individual skills"
  default_prompt: "Use $agent-workspace-checkpoint to ask whether I want to save the complete agent workspace or one validated skill, then ask whether it should remain private or become intentionally public. Keep agent, private, and a generated local key as safe defaults."
