interface:
  display_name: "Agent Workspace Checkpoint"
  short_description: "Connect and create encrypted workspace snapshots"
  default_prompt: "Use $agent-workspace-checkpoint to connect to Relay if needed, then create and locally encrypt a sanitized checkpoint of this project for handoff."
