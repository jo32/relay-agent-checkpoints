interface:
  display_name: "Restore Agent Workspace"
  short_description: "Merge or separately restore Relay checkpoints"
  default_prompt: "Use $restore-agent-workspace to ask whether I want to merge the Relay checkpoint into the current agent workspace or restore it into a separate new workspace, then connect if needed, download, locally decrypt, verify, and complete the chosen restore mode."
