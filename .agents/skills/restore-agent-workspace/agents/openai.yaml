interface:
  display_name: "Restore Agent or Skill"
  short_description: "Restore workspaces or install verified skills"
  default_prompt: "Use $restore-agent-workspace to inspect the Relay artifact type. For an agent, ask whether to merge or restore separately; for a skill, ask which skills root should receive a verified --install-skill restore."
