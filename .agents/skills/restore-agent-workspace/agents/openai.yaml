interface:
  display_name: "Restore Agent Workspace"
  short_description: "Decrypt, verify, and restore Relay checkpoints"
  default_prompt: "Use $restore-agent-workspace to download, locally decrypt, verify, and extract a Relay checkpoint into a new workspace."
